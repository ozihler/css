# Membership Admin UI

Angular 22 standalone application for registering, finding, pausing, and resuming memberships.

```bash
npm ci
npm start
```

The development server proxies `/api` to `http://localhost:9080`. Run `npm run test:ci` for the behavior-focused Vitest suite and `npm run build` for a production bundle.
